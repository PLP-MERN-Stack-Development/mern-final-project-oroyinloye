name: Deploy Frontend to Netlify

on:
  push:
    branches: [ main ]

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: agroconnect-frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies and build
        run: |
          npm ci
          REACT_APP_API_URL=${{ secrets.REACT_APP_API_URL }} npm run build

      - name: Deploy to Netlify
        uses: netlify/actions/cli@master
        with:
          args: deploy --prod --dir=build
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.nfp_61jZscPFQt6gtrsiV3Tn5hYrBhzDzfnV3209 }}
          NETLIFY_SITE_ID: ${{ secrets.d68d396e-2bae-4885-8540-9c83689b0b84 }}
