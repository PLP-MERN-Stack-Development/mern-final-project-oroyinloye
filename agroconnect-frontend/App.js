import Register from './Register';

function App() {
  return (
    <div className="App">
      <Register />
    </div>
  );
}

export default App;
const morgan = require('morgan');
app.use(morgan('dev'));