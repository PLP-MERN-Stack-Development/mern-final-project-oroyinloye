# netlify.toml
[build]
  # set base if your app is in a subfolder, otherwise remove or set to "."
  base = "."
  # change publish to your build output directory, e.g. "dist" or "build"
  publish = "dist"
  # change command to your project's build command
  command = "npm run build"
